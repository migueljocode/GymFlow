<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - GymFlow</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
        .card {
            border-radius: 12px;
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
        }
        .card-header {
            border-radius: 12px 12px 0 0 !important;
        }
        .btn {
            border-radius: 8px;
            padding: 8px 20px;
            font-weight: 500;
            transition: all 0.2s;
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
        }
        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        .btn-success {
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
            border: none;
        }
        .btn-danger {
            background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%);
            border: none;
        }
        .progress {
            border-radius: 10px;
            background-color: #e9ecef;
        }
        .progress-bar {
            border-radius: 10px;
            background: linear-gradient(90deg, #11998e, #38ef7d);
            font-weight: bold;
        }
        .footer {
            background: linear-gradient(135deg, #2c3e50, #3498db);
            color: white !important;
        }
        .footer .text-muted {
            color: rgba(255,255,255,0.8) !important;
        }
        .card {
            animation: fadeIn 0.3s ease-out;
        }
        .table thead th {
            background-color: #2c3e50;
            color: white;
            border: none;
        }
        .wrapper {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        main {
            flex: 1;
        }
        /* Responsive styles */
        .container {
            padding: 0 15px;
        }
        .btn {
            padding: 6px 15px;
            font-size: 14px;
        }
        
        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .table {
            font-size: 14px;
        }
        
        h2, h3, h4, h5 {
            margin-bottom: 0.5rem;
        }
        
        .alert {
            border-radius: 12px;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">💪 GymFlow</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="/">داشبورد</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/WorkoutPlans">برنامه‌های تمرینی</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Workout/Log">ثبت تمرین</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Progress">پیشرفت من</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/Reports">گزارش‌ها</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <main class="container mt-4">
            @RenderBody()
        </main>

        <footer class="footer mt-5 py-3">
            <div class="container text-center">
                <span class="text-muted">GymFlow - مدیریت تمرینات بدنسازی</span>
            </div>
        </footer>
    </div>

    <!-- Bootstrap JS -->
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.js"></script>
    <!-- Custom JS -->
    <script src="~/js/site.js" asp-append-version="true"></script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>